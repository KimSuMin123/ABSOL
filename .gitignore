# 의존성 모듈 (용량이 크고 npm install로 설치 가능하므로 제외)
node_modules/

# 환경 변수 파일 (DB 접속 정보, API 키 등 보안 데이터 포함 - 절대 업로드 금지)
.env

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS 관련 설정 파일
.DS_Store
Thumbs.db

# Sequelize 관련 (로컬 설정이 포함될 수 있는 경우)
/config/config.json (데이터베이스 비번이 들어있다면 제외하거나 .env로 대체 권장)

# 테스트 결과 및 빌드 결과물
dist/
coverage/